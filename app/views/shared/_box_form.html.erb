<%= form_for(@box) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<div class="field">
		<%= f.text_area :content, placeholder: "What's in your bento?" %>
	</div>
	<%= f.submit "Log it!", class: "btn btn-large btn-primary" %>
<% end %>
